mode: closeup

# Trio: a center "boss" flanked by two mirrored backup dancers (the viral
# 'Coming to the Tree' layout). Add a `backup:` block to ANY preset to turn it
# into a trio; remove it for solo. Each distinct character is its own render.

song:
  source: "https://vt.tiktok.com/ZSxEtVRVG/"

theme: "a lush enchanted forest, dense green trees, dappled golden sunlight, drifting mist, magical, cinematic, ultra-detailed, no people"

character:
  image: presets/characters/pink_girl.png   # the boss (centre, large)

backup:
  description: "a bizarre brainrot creature, a fusion of a mushroom and a frog with stubby legs"
